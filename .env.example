# 数据库
DATABASE_URL=postgresql+psycopg2://eldercare:eldercare@localhost:5432/eldercare

# 应用
DEBUG=true
API_V1_PREFIX=/api/v1

# 大模型客户端（OpenAI / 华为云兼容 Chat Completions，供 /risk/safety-check 别名扩展使用）
# 全部留空即降级：别名扩展只用原始输入名，接口仍可正常返回，不报错。
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_TIMEOUT=10.0

